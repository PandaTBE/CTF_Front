import { Route, Switch } from 'react-router-dom';

const App = () => {
    return (
        <>
            <Switch>
                <Route path='/login' />
            </Switch>
        </>
    );
};

export default App;
