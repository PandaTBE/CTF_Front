const host = 'http://localhost:8003/api';

export const urls = {
    HOST: host,
    LOGIN: `${host}/login`,
    LOGOUT: `${host}/logout`,
    REGISTER: `${host}/register`,
    GET_FILES: `${host}`,
    POST_FILE: `${host}`,
    FILE_DOWNLOAD: `${host}/files`,
};
